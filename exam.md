# 基本情報技術者試験　模擬試験

| 項目 | 内容 |
|------|------|
| 試験時間 | 150分 |
| 問題数 | 科目A：20問　科目B：10問 |
| 合格基準 | 科目A・科目B　各600点以上（1000点満点） |

> **受験上の注意**  
> 各問の選択肢（ア〜エ）から最も適切なものを1つ選んでください。  
> 解答・解説は末尾にまとめています。

---

# 科目A　テクノロジ・マネジメント・ストラテジ

---

**問1**　2進数 `1011 0110` を16進数に変換した値はどれか。

```
ア　A6　　イ　B6　　ウ　B5　　エ　A5
```

---

**問2**　2の補数方式で8ビットが表せる整数の範囲はどれか。

```
ア　−128 〜 127　　イ　−127 〜 128　　ウ　−128 〜 128　　エ　−127 〜 127
```

---

**問3**　次の論理式を簡略化したものはどれか。

$$A \cdot B + A \cdot \overline{B}$$

```
ア　A　　イ　B　　ウ　A + B　　エ　1
```

---

**問4**　4ビットのリングカウンタが初期状態 `1000` のとき、クロックパルスを3回入力した後の状態はどれか。

```
ア　0001　　イ　0010　　ウ　0100　　エ　1000
```

---

**問5**　キャッシュメモリのヒット率が0.9、キャッシュのアクセス時間が10 ns、主記憶のアクセス時間が100 ns のとき、実効アクセス時間は何 ns か。

```
ア　10　　イ　19　　ウ　55　　エ　100
```

---

**問6**　OSの仮想記憶管理においてスラッシングが発生する主な原因はどれか。

```
ア　CPUのクロック数が低い
イ　物理メモリが不足し、ページの入替えが頻繁に発生する
ウ　ディスクの回転数が遅い
エ　キャッシュメモリの容量が不足している
```

---

**問7**　「ラウンドロビン方式」の説明として適切なものはどれか。

```
ア　優先度の高いプロセスを常に先に実行する
イ　各プロセスに一定時間（タイムスライス）を割り当て、順番に実行する
ウ　最も短い実行時間のプロセスを優先して実行する
エ　プロセスが完了するまで他のプロセスに切り替えない
```

---

**問8**　第3正規形（3NF）の条件として正しいものはどれか。

```
ア　第1正規形を満たし、すべての非キー属性が主キーに完全関数従属している
イ　第2正規形を満たし、すべての非キー属性が主キーに推移的関数従属していない
ウ　繰り返し項目が存在しない
エ　主キーが単一の属性で構成されている
```

---

**問9**　次のSQL文を実行した結果として返される行数はどれか。

```sql
SELECT DISTINCT department FROM employees WHERE salary > 400000;
```

| department | salary  |
|------------|---------|
| 営業       | 350,000 |
| 開発       | 500,000 |
| 開発       | 600,000 |
| 人事       | 420,000 |
| 営業       | 450,000 |

```
ア　2　　イ　3　　ウ　4　　エ　5
```

---

**問10**　TCPとUDPを比較した説明として適切なものはどれか。

```
ア　UDPはコネクション型プロトコルであり、信頼性の高い通信を行う
イ　TCPはコネクションレス型で、リアルタイム通信に適している
ウ　TCPはコネクション型で信頼性の高い通信を行うが、UDPより遅延が大きくなる場合がある
エ　UDPはフロー制御と輻輳制御を持ち、大容量データ転送に適している
```

---

**問11**　IPv4アドレス `192.168.10.0/24` のネットワークで使用可能なホストアドレスの数はどれか。

```
ア　253　　イ　254　　ウ　255　　エ　256
```

---

**問12**　公開鍵暗号方式の説明として正しいものはどれか。

```
ア　暗号化と復号に同じ鍵を使用する
イ　送信者は受信者の公開鍵で暗号化し、受信者は自分の秘密鍵で復号する
ウ　鍵の配送が共通鍵暗号方式より困難である
エ　処理速度が共通鍵暗号方式より速い
```

---

**問13**　デジタル署名の目的として適切なものはどれか。

```
ア　データの暗号化による盗聴防止
イ　送信者の正当性（なりすまし防止）とデータの改ざん検出
ウ　データの圧縮による転送効率向上
エ　不正アクセスの遮断
```

---

**問14**　オブジェクト指向における「継承（インヘリタンス）」の説明として正しいものはどれか。

```
ア　オブジェクトの内部実装を隠蔽し、外部からはインターフェースのみを公開する
イ　親クラスの属性や操作を子クラスが引き継ぐ仕組み
ウ　同一のメッセージに対してオブジェクトが異なる振る舞いをする
エ　複数のオブジェクトを組み合わせて新しいオブジェクトを作る
```

---

**問15**　「スパイラルモデル」の特徴として適切なものはどれか。

```
ア　要件定義→設計→実装→テストを一方向に進める
イ　小さい単位で開発・評価を繰り返し、リスクを逐次管理しながら開発を進める
ウ　動作するソフトウェアを短いイテレーションで繰り返しリリースする
エ　上流工程に戻ることができないため、要件の変更に弱い
```

---

**問16**　UMLクラス図における「依存関係（Dependency）」の説明として正しいものはどれか。

```
ア　一方のクラスが他方のクラスを属性として保持する
イ　一方のクラスが他方のクラスを継承する
ウ　一方のクラスが他方のクラスを一時的に使用する（メソッドの引数など）
エ　全体と部分の関係を表す
```

---

**問17**　テスト技法の「同値分割法」の説明として適切なものはどれか。

```
ア　入力値の境界となる値を重点的にテストする
イ　入力領域を同じ性質をもつグループに分割し、各グループの代表値でテストする
ウ　すべてのパスを少なくとも1回通過するようにテストケースを設計する
エ　条件分岐のすべての組み合わせをテストする
```

---

**問18**　プロジェクトマネジメントにおける「クリティカルパス」の説明として正しいものはどれか。

```
ア　プロジェクトでコストが最も高くなる作業経路
イ　最も多くのリソースを消費する作業の集合
ウ　余裕時間（フロート）がゼロであり、プロジェクト全体の所要期間を決定する経路
エ　並行して実施可能な作業をまとめた経路
```

---

**問19**　情報セキュリティの「機密性（Confidentiality）」の説明として正しいものはどれか。

```
ア　情報が改ざんされていないことを保証する性質
イ　認可された人だけが情報にアクセスできる性質
ウ　必要なときに情報にアクセスできる性質
エ　情報の送受信を否定できない性質
```

---

**問20**　「ROI（Return on Investment）」を計算する式として正しいものはどれか。

```
ア　投資額 ÷ 利益 × 100（%）
イ　（利益 − 投資額）÷ 投資額 × 100（%）
ウ　利益 ÷ 売上高 × 100（%）
エ　投資額 ÷ 売上高 × 100（%）
```

---

# 科目B　アルゴリズムとプログラミング

---

**問21**　次の疑似コードを実行したとき、出力される値はどれか。

```
x ← 10
y ← 3
z ← x mod y
print(z)
```

```
ア　0　　イ　1　　ウ　3　　エ　7
```

---

**問22**　次の疑似コードを実行したとき、出力される値はどれか。

```
array ← [3, 7, 2, 9, 5]
sum ← 0
i ← 0
while i < 5
    sum ← sum + array[i]
    i ← i + 1
end while
print(sum)
```

```
ア　24　　イ　25　　ウ　26　　エ　27
```

---

**問23**　バブルソートで配列 `[5, 3, 8, 1, 4]` を昇順ソートする際、最初のパスが終了した後の配列はどれか。

```
ア　[1, 3, 4, 5, 8]　　イ　[3, 5, 1, 4, 8]　　ウ　[3, 5, 8, 1, 4]　　エ　[1, 3, 5, 4, 8]
```

---

**問24**　次の再帰関数を `factorial(5)` で呼び出したとき、返される値はどれか。

```
function factorial(n)
    if n <= 1
        return 1
    end if
    return n * factorial(n - 1)
end function
```

```
ア　24　　イ　60　　ウ　120　　エ　720
```

---

**問25**　スタック（LIFO）にデータ A, B, C をこの順でプッシュした後、2回ポップしたとき、スタックに残っているデータはどれか。

```
ア　A　　イ　B　　ウ　C　　エ　スタックは空
```

---

**問26**　二分探索（バイナリサーチ）を適用できる条件として正しいものはどれか。

```
ア　データが連結リストで格納されている
イ　データがソート済みの配列に格納されている
ウ　データが重複していない
エ　データの件数が偶数である
```

---

**問27**　次のPythonコードを実行したとき、出力されるものはどれか。

```python
result = []
for i in range(1, 6):
    if i % 2 == 0:
        result.append(i)
print(result)
```

```
ア　[1, 3, 5]　　イ　[2, 4]　　ウ　[1, 2, 3, 4, 5]　　エ　[2, 4, 6]
```

---

**問28**　次のPythonコードを実行した結果はどれか。

```python
def func(lst):
    lst.append(99)

my_list = [1, 2, 3]
func(my_list)
print(my_list)
```

```
ア　[1, 2, 3]
イ　[1, 2, 3, 99]
ウ　エラーが発生する
エ　[99, 1, 2, 3]
```

---

**問29**　線形探索アルゴリズムの最悪計算量（ビッグオー記法）はどれか。

```
ア　O(1)　　イ　O(log n)　　ウ　O(n)　　エ　O(n²)
```

---

**問30**　次のクラス設計に関する説明として正しいものはどれか。

```python
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        return "..."

class Dog(Animal):
    def speak(self):
        return f"{self.name}がワンと鳴いた"
```

```
ア　Dog クラスは speak メソッドを持たない
イ　Dog クラスの speak は Animal クラスの speak をオーバーライドしている
ウ　Dog インスタンスは name 属性を持たない
エ　Animal クラスをインスタンス化することはできない
```

---

---

# 解答・解説

## 科目A

---

**問1　正解：イ（B6）**

`1011 0110` を4ビットずつ分割する。  
- `1011` = 8+2+1 = 11 → `B`（16進数）  
- `0110` = 4+2 = 6 → `6`  

よって **B6**。

---

**問2　正解：ア（−128 〜 127）**

8ビットの2の補数方式では最上位ビットが符号ビット。  
- 最小値：`1000 0000` = −128  
- 最大値：`0111 1111` = 127

---

**問3　正解：ア（A）**

$$A \cdot B + A \cdot \overline{B} = A \cdot (B + \overline{B}) = A \cdot 1 = A$$

ブール代数の分配法則と補元の法則を適用。

---

**問4　正解：ア（0001）**

リングカウンタは右シフトし、最下位ビットが最上位へ循環する。

| パルス | 状態 |
|--------|------|
| 初期   | 1000 |
| 1回目  | 0100 |
| 2回目  | 0010 |
| 3回目  | 0001 |

---

**問5　正解：イ（19 ns）**

$$実効アクセス時間 = 0.9 \times 10 + 0.1 \times 100 = 9 + 10 = 19 \text{ ns}$$

---

**問6　正解：イ**

スラッシングとは、物理メモリ不足によってページフォールトが頻発し、ページの入替えに大半の時間が費やされ実質的な処理が進まなくなる現象。

---

**問7　正解：イ**

| スケジューリング方式 | 説明 |
|----------------------|------|
| ラウンドロビン       | タイムスライスを均等に割り当て順番に実行（イ） |
| 優先度スケジューリング | 優先度の高いプロセスを先に実行（ア） |
| 最短ジョブ優先（SJF） | 実行時間が最短のプロセスを優先（ウ） |
| ノンプリエンプティブ  | 完了まで切り替えなし（エ） |

---

**問8　正解：イ**

| 正規形 | 条件 |
|--------|------|
| 第1正規形（1NF） | 繰り返し項目なし |
| 第2正規形（2NF） | 1NF + 部分関数従属なし |
| 第3正規形（3NF） | 2NF + **推移的関数従属なし** |

---

**問9　正解：イ（3行）**

`salary > 400000` を満たす行：開発(500,000)・開発(600,000)・人事(420,000)・営業(450,000)  
`DISTINCT department` で重複を排除：**開発・人事・営業** → 3行

---

**問10　正解：ウ**

| プロトコル | 特徴 |
|------------|------|
| TCP | コネクション型・信頼性保証・再送制御・フロー制御あり・低速 |
| UDP | コネクションレス型・信頼性保証なし・低遅延（動画・DNS・VoIPに向く） |

---

**問11　正解：イ（254）**

/24 = サブネットマスク `255.255.255.0`、ホスト部8ビット = 2⁸ = 256  
ネットワークアドレス（.0）とブロードキャストアドレス（.255）を除くと **254ホスト**。

---

**問12　正解：イ**

公開鍵暗号方式の手順：  
1. 送信者が受信者の**公開鍵**で暗号化  
2. 受信者が自分の**秘密鍵**で復号  

共通鍵暗号方式より処理速度は**遅い**。

---

**問13　正解：イ**

デジタル署名は送信者が**秘密鍵**でメッセージのハッシュ値を暗号化したもの。  
受信者は送信者の**公開鍵**で検証する。  
目的：なりすまし防止・改ざん検出・否認防止。

---

**問14　正解：イ**

| 概念 | 説明 |
|------|------|
| カプセル化 | 内部実装の隠蔽（ア） |
| **継承** | 親クラスの属性・操作を子クラスが引き継ぐ（イ） |
| ポリモーフィズム | 同じメッセージへの異なる応答（ウ） |
| 集約・コンポジション | オブジェクトの組み合わせ（エ） |

---

**問15　正解：イ**

| 開発モデル | 特徴 |
|------------|------|
| ウォーターフォール | 一方向に進む（ア・エ） |
| **スパイラル** | 計画→リスク分析→開発→評価を繰り返す（イ） |
| アジャイル | 短いイテレーションで繰り返しリリース（ウ） |

---

**問16　正解：ウ**

| UML関係 | 説明 |
|---------|------|
| 関連（Association） | 属性として保持（ア） |
| 汎化（Generalization） | 継承（イ） |
| **依存（Dependency）** | メソッド引数などの一時的使用、点線矢印（ウ） |
| 集約・合成 | 全体と部分（エ） |

---

**問17　正解：イ**

| テスト技法 | 説明 |
|------------|------|
| 境界値分析 | 境界付近の値をテスト（ア） |
| **同値分割** | 同じ処理が行われるグループの代表値でテスト（イ） |
| パステスト | 全パス網羅（ウ） |
| 判定条件網羅 | 条件の組合せ（エ） |

---

**問18　正解：ウ**

クリティカルパスは**フロート（余裕時間）= 0** のタスクを連ねた経路。  
この経路上のタスクが遅れると、**プロジェクト全体が遅延**する。

---

**問19　正解：イ**

情報セキュリティの3要素（CIA）：

| 要素 | 英語 | 内容 |
|------|------|------|
| **機密性** | Confidentiality | 権限のある者のみがアクセス可能（イ） |
| 完全性 | Integrity | 改ざんされていないこと（ア） |
| 可用性 | Availability | 必要なときにアクセス可能（ウ） |

エは**否認防止（Non-repudiation）**。

---

**問20　正解：イ**

$$ROI = \frac{利益 - 投資額}{投資額} \times 100 \text{（\%）}$$

例：投資額100万円、利益120万円 → ROI = (120−100) ÷ 100 × 100 = **20%**

---

## 科目B

---

**問21　正解：イ（1）**

`mod` は剰余演算。10 ÷ 3 = 3 余り **1**。

---

**問22　正解：ウ（26）**

3 + 7 + 2 + 9 + 5 = **26**

---

**問23　正解：[3, 5, 1, 4, 8]**

バブルソート（昇順）の第1パスの流れ：

| 比較 | 操作 | 状態 |
|------|------|------|
| 5 と 3 | 交換 | [3, 5, 8, 1, 4] |
| 5 と 8 | そのまま | [3, 5, 8, 1, 4] |
| 8 と 1 | 交換 | [3, 5, 1, 8, 4] |
| 8 と 4 | 交換 | [3, 5, 1, 4, **8**] |

最大値の8が末尾に確定する。最も近い選択肢は **イ**。

---

**問24　正解：ウ（120）**

5! = 5 × 4 × 3 × 2 × 1 = **120**

再帰の展開：`factorial(5)` → `5 × factorial(4)` → ... → `5×4×3×2×1`

---

**問25　正解：ア（A）**

スタックはLIFO（後入れ先出し）。

| 操作 | スタック（上→下） |
|------|-------------------|
| A, B, C をプッシュ | C, B, A |
| 1回目ポップ（C取り出し） | B, A |
| 2回目ポップ（B取り出し） | **A** |

---

**問26　正解：イ**

二分探索の前提条件：**データがソート済みであること**。  
中央値と比較しながら探索範囲を半分に絞り込む。計算量は O(log n)。

---

**問27　正解：イ（[2, 4]）**

`range(1, 6)` = 1, 2, 3, 4, 5 のうち `i % 2 == 0`（偶数）は **2, 4**。

---

**問28　正解：イ（[1, 2, 3, 99]）**

Pythonのリストはミュータブル（変更可能）。関数に渡すと**参照渡し**になるため、関数内での変更が元のリストに反映される。

---

**問29　正解：ウ（O(n)）**

| アルゴリズム | 計算量 |
|--------------|--------|
| 定数時間アクセス（配列インデックス） | O(1) |
| 二分探索 | O(log n) |
| **線形探索** | **O(n)** |
| バブルソートなど | O(n²) |

---

**問30　正解：イ**

`Dog` クラスは `Animal` の `speak` メソッドを**オーバーライド（上書き）**している。  
`name` 属性は `Animal.__init__` から継承されるため `Dog` インスタンスも持つ（ウは誤り）。  
`Animal` クラスは抽象クラスではないのでインスタンス化できる（エは誤り）。

---

## 解答一覧

| 問 | 解答 | 問 | 解答 | 問 | 解答 |
|----|------|----|------|----|------|
| 1  | イ   | 11 | イ   | 21 | イ   |
| 2  | ア   | 12 | イ   | 22 | ウ   |
| 3  | ア   | 13 | イ   | 23 | イ   |
| 4  | ア   | 14 | イ   | 24 | ウ   |
| 5  | イ   | 15 | イ   | 25 | ア   |
| 6  | イ   | 16 | ウ   | 26 | イ   |
| 7  | イ   | 17 | イ   | 27 | イ   |
| 8  | イ   | 18 | ウ   | 28 | イ   |
| 9  | イ   | 19 | イ   | 29 | ウ   |
| 10 | ウ   | 20 | イ   | 30 | イ   |

---

*本模擬試験は学習目的で作成されたものです。実際の試験問題とは異なります。*
